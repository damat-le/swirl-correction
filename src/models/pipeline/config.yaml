data_params:
  in_dir: ./data/flower_photos

model_params:
  model_name: pipeline
  num_blocks: 3

corrector_params:
  num_blocks: 3
  in_channels: 3
  out_channels: 3

detector_params:
  in_channels: 3
  out_channels: 1

opt_params:
  lr: 0.001

trainer_params:
  device: 'cuda:1'
  batch_size: 32
  data_workers: 8
  epochs: 1000

log_params:
  log_dir: logs/Pipeline/End2End
  seed: 42

val_params:
  val_size: 370
  batch_size: 64
  data_workers: 4
